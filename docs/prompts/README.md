# docs/prompts/ — Saved OpenCode prompts

**Owner:** You. Saved copies of every non-trivial OpenCode prompt that worked, one `.md` per module, so prompts are reused instead of re-derived (execution plan Part 6).

## Conventions

- Date-stamp each file.
- End every prompt with: "do not modify files outside `<list>`".
- Never let OpenCode generate Hard-Constraint values (retry cap, faithfulness threshold, XBRL concept map, golden-set answers) — it may generate the code structure that uses them.

## Saved prompts

None yet. First expected prompt: Phase 1 `edgar_client.py` spec (execution plan Part 5 table).