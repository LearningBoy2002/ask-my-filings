# Ask My Filings — environment template
# Copy to .env locally and fill in real values. Never commit real values.

# --- Supabase (production storage: Postgres + pgvector + Storage) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_ANON_KEY=

# --- SEC EDGAR ---
# SEC requires a real User-Agent contact string that you control.
SEC_EDGAR_USER_AGENT=

# --- LLM / embeddings (model decision OPEN — see docs/TECH_DECISIONS.md) ---
OPENAI_API_KEY=
COHERE_API_KEY=

# --- Langfuse (observability, AD-009) ---
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=